<odoo>
    <record id="view_inmueble_property_form" model="ir.ui.view">
        <field name="name">inmueble.property.form</field>
        <field name="model">inmueble.property</field>
        <field name="arch" type="xml">
            <form>
                <header>
                    <button string="Confirmar venta" type="action" name="%(action_property_sale_wizard)d"
                            invisible="state != 'offer_accepted'"
                            context="{'default_property_id': id}"/>
                    <button string="Cancelar" type="object" name="action_cancel"
                            invisible="state in ('sold', 'cancelled')"/>
                    <field name="state" widget="statusbar"
                           statusbar_visible="new,offer_received,offer_accepted,sold"/>
                </header>
                <sheet>
                    <group>
                        <field name="name"/>
                        <field name="expected_price" readonly="state != 'new'"/>
                        <field name="selling_price"/>
                        <field name="date_availability"/>
                        <field name="property_type_id"/>
                        <field name="tag_ids" widget="many2many_tags" options="{'color_field': 'color'}"/>
                        <field name="buyer_id"/>
                        <field name="salesperson_id"/>
                    </group>
                    <group>
                        <field name="bedrooms"/>
                        <field name="living_area"/>
                        <field name="garden"/>
                        <field name="garden_area"/>
                        <field name="garden_orientation"/>
                        <field name="total_area"/>
                    </group>
                    <field name="description"/>
                    <notebook>
                        <page string="Ofertas">
                            <field name="offer_ids">
                                <list editable="bottom" decoration-success="status == 'accepted'" decoration-muted="status == 'refused'">
                                    <field name="partner_id"/>
                                    <field name="price"/>
                                    <field name="validity"/>
                                    <field name="date_deadline"/>
                                    <field name="status" invisible="1"/>
                                    <button string="Aceptar" type="object" name="action_accept"
                                            invisible="status in ('accepted', 'refused')"/>
                                    <button string="Rechazar" type="object" name="action_refuse"
                                            invisible="status in ('accepted', 'refused')"/>
                                </list>
                            </field>
                        </page>
                    </notebook>
                </sheet>
                <chatter/>
            </form>
        </field>
    </record>

    <record id="view_inmueble_property_list" model="ir.ui.view">
        <field name="name">inmueble.property.list</field>
        <field name="model">inmueble.property</field>
        <field name="arch" type="xml">
            <list>
                <field name="name"/>
                <field name="expected_price"/>
                <field name="state"/>
            </list>
        </field>
    </record>

    <record id="view_inmueble_property_search" model="ir.ui.view">
        <field name="name">inmueble.property.search</field>
        <field name="model">inmueble.property</field>
        <field name="arch" type="xml">
            <search>
                <field name="name"/>
                <filter name="new" string="Nuevas" domain="[('state', '=', 'new')]"/>
                <group expand="0" string="Agrupar por">
                    <filter name="group_state" string="Estado" context="{'group_by': 'state'}"/>
                </group>
            </search>
        </field>
    </record>

    <record id="action_inmueble_property" model="ir.actions.act_window">
        <field name="name">Propiedades</field>
        <field name="res_model">inmueble.property</field>
        <field name="view_mode">list,form</field>
    </record>

    <menuitem id="menu_inmueble_root" name="Inmobiliaria"/>
    <menuitem id="menu_inmueble_property" name="Propiedades"
              parent="menu_inmueble_root" action="action_inmueble_property"/>
</odoo>
